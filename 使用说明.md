# 眨眼控制 · 使用说明

用眨眼和闭眼来翻短视频，双手不用碰手机也能刷抖音、快手。摄像头画面只在你自己手机上处理，**不保存、不上传**。

---

## 它能做什么

对着手机前置摄像头：

| 动作 | 效果 |
|---|---|
| **快速连眨 3 下** | 翻到 **下一个** 视频（相当于向上滑） |
| **闭眼保持约半秒**（闭着别睁） | 翻到 **上一个** 视频（相当于向下滑） |

> 为什么这么设计：两个动作走的是完全不同的方式（一个数眨眼次数、一个看闭眼时长），所以**多眨几下永远只会是「下一个」，不会翻反方向**。正常看视频时的自然眨眼不会误触发。

---

## 第一次使用（授权三步）

装好打开 App 后，界面上有三项开关，**从上到下**依次打开，每项右边变成 ✅ 就算好了：

1. **摄像头** —— 点「授权摄像头」→ 允许。（用来看你的眨眼，画面不离开手机）
2. **悬浮窗** —— 点「开启悬浮窗权限」→ 在系统页面里把「眨眼控制」允许「显示在其他应用上层」→ 返回。（用来在抖音上面显示一个小状态点）
3. **无障碍** —— 点「去开启无障碍」→ 在列表里找到 **「眨眼控制·滑动」** → 打开 → 系统会弹一个警告（所有这类 App 都有，正常）→ 确认 → 返回。（用来替你完成滑动动作）

三项都 ✅ 后，**「▶ 开始」** 按钮就能点了。

---

## 平时怎么用

1. 点 **「▶ 开始」**，App 会自动退到后台，屏幕上出现一个**可拖动的小胶囊**（状态点）。
2. 小胶囊刚开始显示「学习中」，正常看着屏幕约 **2 秒** 后变「就绪」（绿点）——这是它在熟悉你的眼睛，属正常。
3. 打开**抖音 / 快手**正常刷。
   - **连眨 3 下** → 翻下一个，胶囊闪一下「⬆ 下一个」。
   - **闭眼保持半秒** → 翻上一个，胶囊闪一下「⬇ 上一个」。
4. 想停：拉下通知栏，点「眨眼控制运行中」那条通知里的 **「停止」**；或回到 App 点 **「■ 停止」**。

### 小胶囊上的状态

| 显示 | 意思 |
|---|---|
| 绿点 +「睁 / 闭」 | 正常工作中，实时反映你眼睛睁闭 |
| 琥珀点 +「学习中」 | 刚启动，正在熟悉你的眼睛（约 2 秒） |
| 蓝点 +「⬆下一个 / ⬇上一个」 | 刚刚成功翻了页 |
| **琥珀点 +「卡顿…」** | 手机这会儿资源紧张、画面卡（比如视频正激烈解码），检测可能慢半拍。**等一两秒或慢一点眨**即可，卡过去会自动恢复 |

---

## 用起来更顺的小技巧

- **连眨要「快」**：三下之间别停顿太久，像平时快速眨眼那样连着眨。
- **闭眼翻上一个要「稳」**：闭上后保持住别马上睁，感觉到翻页了再睁。
- **胶囊挡视线**：直接按住它拖到不碍事的角落。
- **看到「卡顿…」**：说明手机正忙，稍等一下再操作，这是提示不是故障。
- **偶尔漏一下**：手机极忙的一两秒里可能漏掉一次连眨，再眨一组就行，不影响正常使用。

---

## 不灵了？多半是手机的后台限制（各品牌都有）

安卓各品牌（小米/红米、华为、OPPO/vivo、荣耀等）默认会限制后台 App。如果出现「切到抖音就不翻页 / 小胶囊消失 / 用一会儿失灵」，到手机**设置**里针对本 App 打开这几项：

- **自启动 / 允许后台活动**（小米叫「自启动」，华为叫「应用启动管理 → 改成手动管理并全部允许」）。
- **省电策略 → 无限制 / 不受限**（关掉对本 App 的省电限制）。
- **允许显示在其他应用上层 / 后台弹出界面**（保住小胶囊）。
- 在最近任务列表里，把本 App **锁住**（下拉出现锁图标），防止一键清理时被关掉。

如果提示「无障碍服务已关闭 / 出现故障」：重新打开 App，它一般会在几秒内自动恢复；若没有，回到授权第 3 步重新开一下无障碍即可。

---

## 隐私

- **全程本地处理**：摄像头画面只喂给你手机里的检测模型算「睁没睁眼」，**不录制、不保存、不上传**。
- **唯一一次联网**：首次启动会下载一个约 3–4MB 的人脸检测模型文件到 App 内部，仅此一次，**只下载、不回传任何画面**。之后可断网使用。

---

## 常见问题

**Q：翻页方向反了？**
默认「连眨 3 下 = 下一个、闭眼保持 = 上一个」。如果你觉得别扭，进 App 设置里可调。

**Q：老是没数够 3 下 / 翻页不够灵敏？**
进 App 设置，把**灵敏度**调高一点，或把**连眨下数**从 3 改小。

**Q：自然眨眼老误翻？**
把灵敏度调低，或把连眨下数调大（比如 4 下）。

**Q：一定要一直插着电脑 / 开着电脑吗？**
不用。装好之后就是一个独立 App，手机自己就能用。
